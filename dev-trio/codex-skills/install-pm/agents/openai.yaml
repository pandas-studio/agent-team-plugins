interface:
  display_name: "dev-trio install-pm"
  short_description: "Codex dev-trio install-pm workflow"
  default_prompt: "Use $install-pm from dev-trio for this workspace."
policy:
  allow_implicit_invocation: false
