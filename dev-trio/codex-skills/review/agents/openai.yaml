interface:
  display_name: "dev-trio review"
  short_description: "Codex dev-trio review workflow"
  default_prompt: "Use $review from dev-trio for this workspace."
policy:
  allow_implicit_invocation: false
